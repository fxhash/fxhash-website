import React, { memo } from 'react';

interface SearchSummaryProps {

}

const _SearchSummary = (props: SearchSummaryProps) => {
  return (
    <div>
search summary
    </div>
  );
};

export const SearchSummary = memo(_SearchSummary);
